import ListButtons from "./ListButtons";

const Header = () => {
  return (
    <header className="header d-flex gap-md flex-column">
      <ListButtons />
      <nav></nav>
    </header>
  );
};

export default Header;
